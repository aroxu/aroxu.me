import React from "react";
import { BrowserRouter } from "react-router-dom";
import Intro from "./Intro";

const Root = () => (
  <BrowserRouter>
    <Intro />
  </BrowserRouter>
);

export default Root;
